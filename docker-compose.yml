version: '3.8'

services:
  EduFordge-client:
    build: 
      context: ./client
      dockerfile: Dockerfile
    container_name: EduFordge-client
    ports:
      - "80:80"
    restart: always
    environment:
      - NODE_ENV=production
